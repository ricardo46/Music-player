import styled from "styled-components";
import {
  INPUT_MAX_WIDTH,
  MAIN_COLOR,
  MOBILE_MAX_WIDTH,
} from "@/globalVariables";

const UserInfo = styled.span`
  display: flex;
`;

export {UserInfo} 